1. 提示符右侧的fx可实时查看函数解析
2. Tab键自动填充
3. diary函数（储存位置另找）
4. 可直接双击工作区变量，直接更改值
5. 等号 (=) 表示赋值。例如：

ls =d
标识符之后的左括号表示函数调用。例如：

ls('./d')
标识符后面的空格（但不在潜在的运算符之后）表示使用命令语法的函数调用。例如：

ls ./d
潜在运算符的两侧带有空格，或者该运算符任意一侧没有空格，均表示变量运算。例如，以下语句是等效的：

ls ./ d ls./d

6. 
